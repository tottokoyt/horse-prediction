# 計画: 測尺データの他クラブ拡張 + モデルA/B/C統合

作成日: 2026-09-23

## 背景

- モデルA（測尺あり）は、シルクの学習データがシルク募集データの
  7重複バグ修正により634頭→184頭に減り、サンプル数不足で精度が
  悪化することが判明した（フェーズ12、上位25%diff +29.0→-12.9pt）。
- 測尺（体高・胸囲・管囲・体重）を外しても現状は精度が落ちない
  （むしろ僅かに良い: CV AUC 測尺なし0.875 vs 測尺あり0.869〜0.872、
  性別正規化・比率・現場の経験則的閾値フラグを追加しても変わらず）。
  ただしn=184は統計的検出力が弱く、この結果だけで「測尺は無意味」
  と断定はできない。
- モデルB（測尺なし・汎用版）は、実質的にモデルC（クラブ横断汎用
  モデル）に置き換わっており、単独維持する意義が薄い。

## 方針（ユーザー承認済み）

1. モデルBは廃止し、モデルC（クラブ横断学習）に一本化する
2. モデルAの「測尺」を、モデルCのクラブ横断学習プールに
   "あれば使う追加特徴量"として組み込む（他クラブはNaN、
   測尺データがあるクラブだけ実値が入る）
3. そのために、シルク以外のクラブからも測尺データを追加収集する

## 他クラブの測尺データ公開状況（今夜、実データを直接確認して検証済み）

既存の「other」10クラブについて、公式サイトの測尺公開状況を
WebSearchの要約だけでなく**実際にページ・PDFの中身を取得して
検証した**（検索結果の要約だけを信用すると誤った結論になることが
raffian/winの調査で判明したため、全クラブ実データで確認した）。

| クラブ | 公開状況 | 詳細 |
|---|---|---|
| carrot（キャロット） | ✅確認済み・公開（HTML表） | `carrotclub.net/horse/{year}_size_farm.asp`等、体高/胸囲/管囲/体重の4項目フルセット、91頭/年確認 |
| normandy（ノルマンディー） | ✅確認済み・公開（HTML表） | `normandyoc.com/sp/collect/height?nenrei=N`、4項目フルセット確認 |
| union（ユニオン） | ✅確認済み・公開（個別馬ページ） | `union-oc.co.jp/id/{ID}`、4項目フルセット確認（体高153cm 胸囲170cm 管囲19.8cm 体重398kg 等） |
| raffian（ラフィアン） | ❌**公開なし（要修正）** | 個別馬ページの「カタログPDF」を実際に開いて確認したところ、中身は血統表のみで測尺データは含まれていなかった |
| win（ウイン） | ❌**公開なし（要修正）** | 同上。PDFは血統カタログのみ。HTMLページには体重のみ（おそらく近況値で募集時測尺ではない）が表示されるが、体高・胸囲・管囲は無い |
| road（ロード） | 🟡**公開の実績はあるが要再調査** | ブログ記事形式で過去複数年分「測尺値」記事が存在した形跡（2021〜2025年分のタイトルを検索で確認）が、サンプルURLはいずれも404で現在は閲覧できず。サイト内検索・archive.org等で現在の記事URLを再特定する必要あり |
| shadai（社台） | ❌会員限定ログイン必須 | 収集不可 |
| sunday（サンデー） | ❌会員限定ログイン必須 | 収集不可 |
| g1（Ｇ１） | ❌会員限定ログイン必須 | 収集不可 |
| tokyo（東京サラブレッド） | ❌会員限定ログイン必須（一部近況体重のみ公開だが募集時測尺ではない） | 収集不可 |

**確定した見込み**: シルク＋確認済み3クラブ（carrot, normandy,
union）から測尺データを追加できる。road は再調査すれば追加できる
可能性がある（4クラブ目）。raffian・winは断念（公開データなし）。
shadai/sunday/g1/tokyoの4クラブは会員限定のため収集不可
（ログイン情報を使った会員限定コンテンツへのアクセスは行わない）。

**当初の見立て（最大6クラブ）から下方修正: 確実に取れるのは3クラブ、
運が良ければ4クラブ。** それでもシルク単独(184頭)よりはサンプルが
増える見込み（carrot/normandy/unionの規模次第、まだ頭数は未集計）。

## フェーズ1の結果（2026-09-23 日中セッション）

**重要な前提の訂正**: 現行の公式サイトは「募集中の世代」か「現役馬の現在の
測尺」しか出しておらず、学習年度(2018-2021募集)の**募集時**測尺は
どのクラブも現行サイトからは取れない。取得元はすべて Wayback Machine。

| クラブ | 結果 | 学習年度の取得数 | 手法・注意点 |
|---|---|---|---|
| carrot | ✅既に取得済みだった（計画書に記載漏れ） | 242頭（2018/2019/2021）、**2020年度は無し** | 9/22に `archive/fetch_carrot_scale.py` + `merge_carrot_scale.py` で取得済み（`other_scale_cache.csv`）。2020年度は一覧ページがWaybackに無く、個別ページ `bosyuba.asp?id=20xx` は残っているが測尺欄が無い |
| normandy | ✅取得完了 | **159/198頭**（2018:43/44, 2019:47/47, 2020:40/49, 2021:29/58） | `fetch_normandy_scale.py`（新規）。`/collect/height.aspx` の月次キャプチャ19件を合算し、各馬の最も早い値を採用。4項目フルセット |
| union | ✅取得完了 | **160/168頭**（2018:40/41, 2019:38/40, 2020:41/43, 2021:41/44） | `fetch_union_scale.py`（新規）。個別馬ページ `/id/NNNN`（ID 3150〜3700）219件。**体高・胸囲・管囲の3項目のみで体重なし**。計測は**募集年の6月**（carrot/normandyの秋計測より数か月早く、平均値も一回り小さい）。デビュー後キャプチャで競走馬名も確認できた40頭は馬名・性別とも全一致 |
| road | ❌断念 | 0 | 公式は `lord-to.co.jp`。測尺記事は募集終了後に削除される運用（2025年分も既に404）。募集馬PDF(`wp-content/uploads/2020/11/19NN.pdf`等)はWaybackにあるが**1MBで切り詰めて保存**されており（元は1.6〜2.2MB）、先頭部分にテキストも無い（画像主体）。umadb.com にも測尺は無い |

**補足: 過去の「carrot測尺は効果なし」の結論は再検証が必要。**
v10〜v12（README「他クラブ測尺統合の実験」）の比較は、シルク7重複バグ修正前の
634頭をベースラインにしていたため、今回の修正後データでは結論が変わりうる。

## フェーズ4の結果（2026-09-23 日中セッション、本番未反映）

学習プール2,472頭のうち測尺つき717頭（silk 184, carrot 227, normandy 156, union 150）。
現行モデルC（log1p huber + lambdarank）に測尺を足して真のLOCOで検証した。

1. `experiment_measurement_loco.py`（13クラブ×5seed、なし/生値/標準化/シャッフル）
   - 測尺の**全く無い**クラブ（g1/sunday/tclion）でも上位25%diffが±5pt動いた
     → 5seedではクラブ単位の差がノイズと区別できない
   - そこで**プラセボ対照**（クラブ×年度内で測尺値を馬の間でシャッフル、欠損位置は保持）を追加
2. `experiment_measurement_placebo.py`（測尺4クラブ×20試行）

| 指標（4クラブ平均） | なし | 標準化 | シャッフル | 標準化−なし（対応あり） |
|---|---|---|---|---|
| 上位25%diff | 4.0 | **8.5** | 4.2 | **+4.5±0.7pt、20/20試行で上回る** |
| spearman | 0.045 | 0.052 | 0.023 | +0.007±0.004、14/20 |

- 上位25%diffの改善はプラセボでは出ない（+0.2±0.7）→ **測尺の中身による改善**
- クラブ別: silk -1.2→8.4、normandy 5.5→9.2、union 7.3→9.4、carrot 4.6→7.1
  （ただし carrot はシャッフルの方が11.5と高く、説明できていない）
- union の spearman は低下（0.075→0.018）するが、シャッフルでも同様に低下するので
  測尺値ではなく「union だけ体重が欠損」という構造の影響とみられる
- 標準誤差は seed 間のばらつきのみで、評価対象の馬（4クラブ≒850頭）の標本誤差は
  含まない。**証拠の強さは「中程度」**

### 追試: 生値（選択肢(c)）の20試行（同じseed・同じシャッフルで比較）

| 指標（4クラブ平均） | なし | 生値 | 標準化 | シャッフル |
|---|---|---|---|---|
| 上位25%diff | 4.0 | 7.7（+3.6±0.7、17/20） | 8.5（+4.5±0.7、20/20） | 4.2 |
| spearman | 0.045 | 0.045（±0、10/20） | 0.052（+0.007、14/20） | 0.023 |

- 標準化−生値: 上位25%diff +0.9±0.6（14/20）、spearman +0.007±0.003（15/20）→ 標準化がやや上だが差は小さい
- クラブ別で差が大きいのは silk（上位25%diff 生値0.6 vs 標準化8.4）。carrot・normandy は逆に生値の方が高い
- 補足: 木モデルは全頭共通の単調変換で結果が変わらないので、選択肢(b)を
  「全体で1つの平均・標準偏差」で行うと(c)と完全に同じになる。(b)が(c)と違う結果になるのは、
  基準をクラブ別・計測月別に分けた場合だけ

### 決定と実装（2026-09-23、ユーザー承認: (c) 生値を採用）

- `train_model_kakutoku.py`: `load_combined()` に測尺4列（+bosyu_year）を追加、`FEATURE_BASE` に測尺4列を追加。
  学習プールの測尺付与は検証時と完全一致（2,472頭中717頭）
- `models/lgbm_model_kakutoku.pkl` を再学習（旧版は `data/_backup_pre_dedup_fix_20260923/lgbm_model_kakutoku.before_measurement.pkl`）。
  同じランダムホールドアウト(370頭中測尺あり100頭)で アンサンブル上位25%差分 7.1±3.4 → 10.5±4.4
- `app/predictor.py`: 測尺を1項目ずつ扱う（体重なし等の部分入力可）、モデルCのファクター一覧に体高/胸囲/管囲/体重を追加
- `app/static/index.html`: 測尺欄の説明と、クラブ横断モデルの説明文に測尺の効果と限界を追記
- `experiment_measurement_loco.py`: 本番変更後も同じ条件で再現できるよう調整
- **未実施**: git commit・push・Render デプロイ（ユーザー確認待ち）、モデルA/B の廃止

### （参考）採用前に検討した選択肢

- **推論時の標準化の基準をどうするか**: 標準化版は「同じクラブ・同じ世代の中での相対値」を
  使うので、予測したい馬の世代の全頭（少なくとも十数頭）の測尺が必要。本来の目的の
  バヌーシーは学習プールに測尺が1頭も無い。選択肢:
  (a) 推論時に同世代の全頭の測尺を入力してもらう
  (b) 学習プール全体（計測月別）の平均・標準偏差を固定の基準として使う
  (c) 生値版を使う（5seed試験では上位25%diff 7.7と標準化と同等だったが、20試行の追試は未実施）
- `app/predictor.py` の入力フォーム・SHAP表示の変更範囲

## 実施ステップ（次回セッション以降）

### フェーズ1: 対象年度カバレッジの確認
- carrot, normandy, unionそれぞれについて、学習年度(2018-2021)分の
  ページ/データが遡って取得できるか確認する（carrotは少なくとも
  2021年度ページの存在を確認済み、normandy/unionは年度別URLの
  パターンをさらに確認する必要あり）
- roadは、まず公式サイト内検索かサイトマップで現在の「測尺値」
  記事URLを再特定してから判断する

### フェーズ2: クラブごとのスクレイパー作成
- carrot, normandy, union: いずれもHTML表/個別ページなので
  比較的簡単（collect_jisseki_other.pyのパターンを流用可能）
- road: ブログ記事形式のため、記事本文からのパース処理が必要
  （表になっているかは要確認、写真+テキストの場合は抽出が難しい
  可能性もある）
- 各クラブのサイトのrobots.txt・利用規約を確認し、Crawl-delay等の
  マナーを守る（silkhorseclub.jpと同様、fetch_sire_other.pyで
  対応したClaudeBotのCrawl-delay:30のような制約が無いか要確認）

### フェーズ3: データ統合・モデル再設計
- `train_model_kakutoku.py`の`load_combined()`に、測尺4列
  （height/chest/cannon/weight）を追加。データが無いクラブは
  NaNのまま（LightGBMは欠損値を扱える）
- `get_feature_cols()`に測尺4列を追加
- 学習・推論（`app/predictor.py`）両方を更新

### フェーズ4: 検証・判断
- 今夜確立した「真のLOCO検証」手法（11クラブ全部を1つずつ未知として
  扱う、5 seed）で、測尺追加の効果を検証
- 効果が確認できれば本番採用、モデルA/Bは廃止しモデルCに一本化
- 効果が確認できなければ（サンプル数を増やしても測尺が効かない
  ことが分かれば）、測尺機能自体を諦めるという判断もあり得る

## 今夜やったこと・やらなかったこと

- ✅ 測尺の特徴量エンジニアリング（性別正規化・比率・閾値フラグ）の
  効果検証（シルク単独データでは効果確認できず）
- ✅ 10クラブ全部について、公式サイト・PDFの中身を実際に取得して
  測尺公開状況を検証（WebSearchの要約だけでなく実データで確認。
  raffian/winは検索結果からは公開ありそうに見えたが、実際に
  PDFを開いたら血統カタログのみで測尺は入っていなかった、という
  重要な訂正あり）
- ✅ 確実に使える3クラブ（carrot, normandy, union）を確定
- ❌ 各クラブの対象年度(2018-2021)カバレッジ確認（未着手）
- ❌ 実際のスクレイピング・データ収集（未着手、次回以降）
- ❌ モデルB廃止・モデルC統合の実装（未着手、次回以降）

本番モデル・コードへの変更はこの件については一切行っていない。
